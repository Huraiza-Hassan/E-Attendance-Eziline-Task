import 'package:flutter/material.dart';

//widget to add vertical space to screen
Widget addVerticalSpace(double height) {
  return SizedBox(
    height: height,
  );
}

//widget to add horizontal space to screen
Widget addHorizontalSpace(double width) {
  return SizedBox(
    width: width,
  );
}
